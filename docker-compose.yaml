version: "3.3"
services:
  jekyll:
    image: bretfisher/jekyll-serve
    volumes:
      - .:/site
      # - ./vendor/bundle/:/usr/local/bundle
    ports:
      - "4000:4000"
    # command: bretfisher/jekyll-serve

